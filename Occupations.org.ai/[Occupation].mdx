---
$id: https://graph.org.ai/occupations/{code}
$type: Occupation
source: O*NET
code: "{code}"
title: "{title}"
---

export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => {
      return rows.map(row => {
          const code = row['O*NET-SOC Code'];
          const title = row['Title'];

          return {
              id: code,
              slug: code,
              code: code,
              title: title,
              description: row['Description'],
              $id: `https://graph.org.ai/occupations/${code}`
          };
      });
  });

# {title}

**O*NET-SOC Code**: {code}

{description}

## Related Data

Browse the folder structure to explore:
- **Tasks**: Specific work activities for this occupation
- **Technology**: Software and technology skills used
- **Tools**: Physical tools and equipment used
