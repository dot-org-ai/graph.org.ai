---
id: ProtectiveServiceWorkersAllOther
slug: ProtectiveServiceWorkersAllOther
code:
  code: null
title:
  title: null
description: All protective service workers not listed separately.
$id: https://graph.org.ai/occupations/ProtectiveServiceWorkersAllOther
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$ProtectiveServiceWorkersAllOther`
      };
  }));

# Protective Service Workers, All Other

**O*NET Code**: 33-9099.00

All protective service workers not listed separately.

## Job Profile
<JobZone occupation=ProtectiveServiceWorkersAllOther />

## Work Activities
### Core Tasks
<Tasks occupation=ProtectiveServiceWorkersAllOther />

### General Activities
<Activities occupation=ProtectiveServiceWorkersAllOther />

## Qualifications
### Skills
<Skills occupation=ProtectiveServiceWorkersAllOther />

### Knowledge
<Knowledge occupation=ProtectiveServiceWorkersAllOther />

### Abilities
<Abilities occupation=ProtectiveServiceWorkersAllOther />

## Work Context
<WorkContext occupation=ProtectiveServiceWorkersAllOther />

## Labor Market
### Wages & Trends
<Wages occupation=ProtectiveServiceWorkersAllOther />

### Industries
<Industries occupation=ProtectiveServiceWorkersAllOther />