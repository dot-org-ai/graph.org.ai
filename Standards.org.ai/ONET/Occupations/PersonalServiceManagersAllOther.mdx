---
id: PersonalServiceManagersAllOther
slug: PersonalServiceManagersAllOther
code:
  code: null
title:
  title: null
description: All personal service managers not listed separately.
$id: https://graph.org.ai/occupations/PersonalServiceManagersAllOther
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$PersonalServiceManagersAllOther`
      };
  }));

# Personal Service Managers, All Other

**O*NET Code**: 11-9179.00

All personal service managers not listed separately.

## Job Profile
<JobZone occupation=PersonalServiceManagersAllOther />

## Work Activities
### Core Tasks
<Tasks occupation=PersonalServiceManagersAllOther />

### General Activities
<Activities occupation=PersonalServiceManagersAllOther />

## Qualifications
### Skills
<Skills occupation=PersonalServiceManagersAllOther />

### Knowledge
<Knowledge occupation=PersonalServiceManagersAllOther />

### Abilities
<Abilities occupation=PersonalServiceManagersAllOther />

## Work Context
<WorkContext occupation=PersonalServiceManagersAllOther />

## Labor Market
### Wages & Trends
<Wages occupation=PersonalServiceManagersAllOther />

### Industries
<Industries occupation=PersonalServiceManagersAllOther />