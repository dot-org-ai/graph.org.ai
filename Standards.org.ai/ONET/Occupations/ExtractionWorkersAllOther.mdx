---
id: ExtractionWorkersAllOther
slug: ExtractionWorkersAllOther
code:
  code: null
title:
  title: null
description: All extraction workers not listed separately.
$id: https://graph.org.ai/occupations/ExtractionWorkersAllOther
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$ExtractionWorkersAllOther`
      };
  }));

# Extraction Workers, All Other

**O*NET Code**: 47-5099.00

All extraction workers not listed separately.

## Job Profile
<JobZone occupation=ExtractionWorkersAllOther />

## Work Activities
### Core Tasks
<Tasks occupation=ExtractionWorkersAllOther />

### General Activities
<Activities occupation=ExtractionWorkersAllOther />

## Qualifications
### Skills
<Skills occupation=ExtractionWorkersAllOther />

### Knowledge
<Knowledge occupation=ExtractionWorkersAllOther />

### Abilities
<Abilities occupation=ExtractionWorkersAllOther />

## Work Context
<WorkContext occupation=ExtractionWorkersAllOther />

## Labor Market
### Wages & Trends
<Wages occupation=ExtractionWorkersAllOther />

### Industries
<Industries occupation=ExtractionWorkersAllOther />