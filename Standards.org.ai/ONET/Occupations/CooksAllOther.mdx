---
id: CooksAllOther
slug: CooksAllOther
code:
  code: null
title:
  title: null
description: All cooks not listed separately.
$id: https://graph.org.ai/occupations/CooksAllOther
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$CooksAllOther`
      };
  }));

# Cooks, All Other

**O*NET Code**: 35-2019.00

All cooks not listed separately.

## Job Profile
<JobZone occupation=CooksAllOther />

## Work Activities
### Core Tasks
<Tasks occupation=CooksAllOther />

### General Activities
<Activities occupation=CooksAllOther />

## Qualifications
### Skills
<Skills occupation=CooksAllOther />

### Knowledge
<Knowledge occupation=CooksAllOther />

### Abilities
<Abilities occupation=CooksAllOther />

## Work Context
<WorkContext occupation=CooksAllOther />

## Labor Market
### Wages & Trends
<Wages occupation=CooksAllOther />

### Industries
<Industries occupation=CooksAllOther />