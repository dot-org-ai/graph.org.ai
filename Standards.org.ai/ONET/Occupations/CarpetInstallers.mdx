---
id: CarpetInstallers
slug: CarpetInstallers
code:
  code: null
title:
  title: null
description: Lay and install carpet from rolls or blocks on floors. Install
  padding and trim flooring materials.
$id: https://graph.org.ai/occupations/CarpetInstallers
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$CarpetInstallers`
      };
  }));

# Carpet Installers

**O*NET Code**: 47-2041.00

Lay and install carpet from rolls or blocks on floors. Install padding and trim flooring materials.

## Job Profile
<JobZone occupation=CarpetInstallers />

## Work Activities
### Core Tasks
<Tasks occupation=CarpetInstallers />

### General Activities
<Activities occupation=CarpetInstallers />

## Qualifications
### Skills
<Skills occupation=CarpetInstallers />

### Knowledge
<Knowledge occupation=CarpetInstallers />

### Abilities
<Abilities occupation=CarpetInstallers />

## Work Context
<WorkContext occupation=CarpetInstallers />

## Labor Market
### Wages & Trends
<Wages occupation=CarpetInstallers />

### Industries
<Industries occupation=CarpetInstallers />