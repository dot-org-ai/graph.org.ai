---
id: PestControlWorkers
slug: PestControlWorkers
code:
  code: null
title:
  title: null
description: Apply or release chemical solutions or toxic gases and set traps to
  kill or remove pests and vermin that infest buildings and surrounding areas.
$id: https://graph.org.ai/occupations/PestControlWorkers
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$PestControlWorkers`
      };
  }));

# Pest Control Workers

**O*NET Code**: 37-2021.00

Apply or release chemical solutions or toxic gases and set traps to kill or remove pests and vermin that infest buildings and surrounding areas.

## Job Profile
<JobZone occupation=PestControlWorkers />

## Work Activities
### Core Tasks
<Tasks occupation=PestControlWorkers />

### General Activities
<Activities occupation=PestControlWorkers />

## Qualifications
### Skills
<Skills occupation=PestControlWorkers />

### Knowledge
<Knowledge occupation=PestControlWorkers />

### Abilities
<Abilities occupation=PestControlWorkers />

## Work Context
<WorkContext occupation=PestControlWorkers />

## Labor Market
### Wages & Trends
<Wages occupation=PestControlWorkers />

### Industries
<Industries occupation=PestControlWorkers />