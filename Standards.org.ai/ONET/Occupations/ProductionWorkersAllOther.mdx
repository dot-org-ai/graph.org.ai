---
id: ProductionWorkersAllOther
slug: ProductionWorkersAllOther
code:
  code: null
title:
  title: null
description: All production workers not listed separately.
$id: https://graph.org.ai/occupations/ProductionWorkersAllOther
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$ProductionWorkersAllOther`
      };
  }));

# Production Workers, All Other

**O*NET Code**: 51-9199.00

All production workers not listed separately.

## Job Profile
<JobZone occupation=ProductionWorkersAllOther />

## Work Activities
### Core Tasks
<Tasks occupation=ProductionWorkersAllOther />

### General Activities
<Activities occupation=ProductionWorkersAllOther />

## Qualifications
### Skills
<Skills occupation=ProductionWorkersAllOther />

### Knowledge
<Knowledge occupation=ProductionWorkersAllOther />

### Abilities
<Abilities occupation=ProductionWorkersAllOther />

## Work Context
<WorkContext occupation=ProductionWorkersAllOther />

## Labor Market
### Wages & Trends
<Wages occupation=ProductionWorkersAllOther />

### Industries
<Industries occupation=ProductionWorkersAllOther />