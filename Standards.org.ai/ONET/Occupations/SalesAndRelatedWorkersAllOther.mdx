---
id: SalesAndRelatedWorkersAllOther
slug: SalesAndRelatedWorkersAllOther
code:
  code: null
title:
  title: null
description: All sales and related workers not listed separately.
$id: https://graph.org.ai/occupations/SalesAndRelatedWorkersAllOther
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$SalesAndRelatedWorkersAllOther`
      };
  }));

# Sales and Related Workers, All Other

**O*NET Code**: 41-9099.00

All sales and related workers not listed separately.

## Job Profile
<JobZone occupation=SalesAndRelatedWorkersAllOther />

## Work Activities
### Core Tasks
<Tasks occupation=SalesAndRelatedWorkersAllOther />

### General Activities
<Activities occupation=SalesAndRelatedWorkersAllOther />

## Qualifications
### Skills
<Skills occupation=SalesAndRelatedWorkersAllOther />

### Knowledge
<Knowledge occupation=SalesAndRelatedWorkersAllOther />

### Abilities
<Abilities occupation=SalesAndRelatedWorkersAllOther />

## Work Context
<WorkContext occupation=SalesAndRelatedWorkersAllOther />

## Labor Market
### Wages & Trends
<Wages occupation=SalesAndRelatedWorkersAllOther />

### Industries
<Industries occupation=SalesAndRelatedWorkersAllOther />