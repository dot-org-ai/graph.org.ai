---
id: CooksShortOrder
slug: CooksShortOrder
code:
  code: null
title:
  title: null
description: Prepare and cook to order a variety of foods that require only a
  short preparation time. May take orders from customers and serve patrons at
  counters or tables.
$id: https://graph.org.ai/occupations/CooksShortOrder
$type: Occupation
source: ONET
---
export const items = CSV.fetch('https://www.onetcenter.org/dl_files/database/db_30_0_text/Occupation%20Data.txt')
  .then(rows => rows
      .filter(row => row['Title'] && row['O*NET-SOC Code'])
      .map(row => {
      const title = row['Title'];
      const code = row['O*NET-SOC Code'];
      const description = row['Description'];
      // PascalCase for Component/File naming
      const slug = _.upperFirst(_.camelCase(title));
      
      return {
          id: slug, // Filename
          slug: slug,
          code: code,
          title: title,
          description: description,
          $id: `https://graph.org.ai/occupations/$CooksShortOrder`
      };
  }));

# Cooks, Short Order

**O*NET Code**: 35-2015.00

Prepare and cook to order a variety of foods that require only a short preparation time. May take orders from customers and serve patrons at counters or tables.

## Job Profile
<JobZone occupation=CooksShortOrder />

## Work Activities
### Core Tasks
<Tasks occupation=CooksShortOrder />

### General Activities
<Activities occupation=CooksShortOrder />

## Qualifications
### Skills
<Skills occupation=CooksShortOrder />

### Knowledge
<Knowledge occupation=CooksShortOrder />

### Abilities
<Abilities occupation=CooksShortOrder />

## Work Context
<WorkContext occupation=CooksShortOrder />

## Labor Market
### Wages & Trends
<Wages occupation=CooksShortOrder />

### Industries
<Industries occupation=CooksShortOrder />